name: Trigger Daily - Parallel (cypress-split)

env:
  GOOGLE_EMAIL: ${{ secrets.GOOGLE_EMAIL }}
  GOOGLE_CLIENTID: ${{ secrets.GOOGLE_CLIENTID }}
  GOOGLE_CLIENT_SECRET: ${{ secrets.GOOGLE_CLIENT_SECRET }}
  GOOGLE_REFRESH_TOKEN: ${{ secrets.GOOGLE_REFRESH_TOKEN }}

  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # Cypress
  CYPRESS_RECORD_KEY: ${{ vars.CYPRESS_RECORD_KEY }}
  PROJECT_ID: ${{ vars.PROJECT_ID }}

  # Links
  DEV_APP_URL: ${{ vars.DEV_APP_URL }}
  PROD_APP_URL: ${{ vars.PROD_APP_URL }}
  DEV_MARKETING_SITE_URL: ${{ vars.DEV_MARKETING_SITE_URL }}
  DEV_API_URL: ${{ vars.DEV_API_URL }}
  APP_EMAIL_SENDER: ${{ vars.APP_EMAIL_SENDER }}

  # Add credit card data
  AUTHORIZE_DEV_API_LOGIN_ID: ${{ secrets.AUTHORIZE_DEV_API_LOGIN_ID }}
  AUTHORIZE_DEV_TRANSACTION_KEY: ${{ secrets.AUTHORIZE_DEV_TRANSACTION_KEY }}

on:
  workflow_dispatch:
  schedule:
    - cron: '0 8 * * *'
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
jobs:
  prepare-accounts-data:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout 🛎
        uses: actions/checkout@v4

      - name: Print cypress info 🖨
        run: npx cypress info

      - name: Print node version 🖨
        run: node -v

      - name: Print OS and CPUs 🖨
        run: node -p 'os.cpus()'

      - name: Check file existence 🖨
        run: npm run check-file-existence

      - name: Create "Lender" Account for testing 🧪
        run: npm run create-acc-by:lender

      - name: Create "Borrower" Accounts for testing 🧪
        run: npm run create-acc-by:borrower

      - name: Create "Partner" Accounts for testing 🧪
        run: npm run create-acc-by:partner

      - name: Create "Team Member" Accounts for testing 🧪
        run: npm run create-acc-by:team-member

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: prepare-accounts-data
          path: ./

  # ACCOUNTS
  create-accounts:
    needs: prepare-accounts-data
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false

    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          name: prepare-accounts-data

      - name: Remove package-lock.json if exists
        run: rm -f package-lock.json

      - name: Run creating main Lender account 🧪
        # https://github.com/cypress-io/github-action
        uses: cypress-io/github-action@v6
        with:
          browser: chrome
          spec: 'cypress/e2e/trigger/service/prepare-accounts.cy.js' # path to the prepare-accounts

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: create-accounts
          path: ./

  prepare:
    needs: [create-accounts]
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.prepare.outputs.matrix }}

    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          name: create-accounts

      - name: Print .env
        run: echo "DEV_MARKETING_SITE_URL => ${{ vars.DEV_MARKETING_SITE_URL }}"

      # generate the list using a bash script
      - name: Create matrix ⊹
        id: prepare
        # for reusable workflow, must use the full action reference
        uses: bahmutov/gh-build-matrix@main
        with:
          n: 10 # number of containers to output

      - name: Print result 🖨
        run: echo '${{ steps.prepare.outputs.matrix }}'

      - name: Install dependencies
        run: npm install

      - name: Grant execute permissions to Cypress
        run: chmod +x ./node_modules/.bin/cypress

      - name: Print cypress info 🖨
        run: npx cypress info

      - name: Print node version 🖨
        run: node -v

      - name: Print OS and CPUs 🖨
        run: node -p 'os.cpus()'

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: prepare
          path: ./

  test-split:
    needs: prepare
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix: ${{ fromJSON(needs.prepare.outputs.matrix) }}

    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          name: prepare

      - name: Print GitHub variables 🖨
        run: npx @bahmutov/print-env GITHUB

      - name: Print GitHub strategy context 🖨
        run: echo '${{ toJSON(strategy) }}'

      - name: Run split Cypress tests 🧪
        # https://github.com/cypress-io/github-action
        uses: cypress-io/github-action@v6
        with:
          browser: chrome
        env:
          SKIP_SPEC: 'cypress/e2e/trigger/service/**.cy.js' # path to the prepare-accounts file
          SPLIT: ${{ strategy.job-total }}
          SPLIT_INDEX: ${{ strategy.job-index }}
          DEBUG: cypress-split
